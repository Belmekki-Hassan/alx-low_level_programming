#include "main.h"
#include <stdio.h>
/**
 * main - Display the name of the program
 * @argc: nbr of cli argument
 * @argv: list name
 * Return: 0
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc * 0]);
return (0);
}
